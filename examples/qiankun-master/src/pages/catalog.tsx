export default function CatalogPage() {
  return (
    <section className="panel">
      <p className="eyebrow">qiankun master</p>
      <h1>Catalog workspace</h1>
      <p>
        The catalog slave is mounted below when the master resolver activates
        the child app for <code>/catalog</code>.
      </p>
    </section>
  );
}
